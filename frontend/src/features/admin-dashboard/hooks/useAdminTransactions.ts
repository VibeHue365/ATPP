import { useCallback,useEffect,useState } from 'react'; import { adminTransactionsApi,type TransactionPage } from '../api/adminTransactionsApi';
export function useAdminTransactions(){const[page,setPage]=useState(1);const[data,setData]=useState<TransactionPage|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const refresh=useCallback(async()=>{setLoading(true);setError(null);try{setData(await adminTransactionsApi.list(page))}catch(e){setError(e instanceof Error?e.message:'Không thể tải giao dịch.')}finally{setLoading(false)}},[page]);useEffect(()=>{let alive=true;const load=async()=>{try{const next=await adminTransactionsApi.list(page);if(alive)setData(next)}catch(e){if(alive)setError(e instanceof Error?e.message:'Không thể tải giao dịch.')}finally{if(alive)setLoading(false)}};void load();return()=>{alive=false}},[page]);return{page,setPage,data,loading,error,refresh}}