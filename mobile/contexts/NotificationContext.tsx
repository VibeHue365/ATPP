import { createContext,useCallback,useContext,useEffect,useMemo,useState,type PropsWithChildren } from 'react';
import { notificationApi } from '@/apis/notificationApi';import { useAuth } from './AuthContext';import type { NotificationItem } from '@/types/notification';
import { getApiErrorMessage } from '@/utils/apiError';
interface Value{notifications:NotificationItem[];unreadCount:number;loading:boolean;error:string|null;reload:()=>Promise<void>;markRead:(id:string)=>Promise<void>;markAllRead:()=>Promise<void>}
const Context=createContext<Value|null>(null);
export function NotificationProvider({children}:PropsWithChildren){const {isAuthenticated}=useAuth();const [notifications,setNotifications]=useState<NotificationItem[]>([]);const [loading,setLoading]=useState(false);const [error,setError]=useState<string|null>(null);const reload=useCallback(async()=>{if(!isAuthenticated){setNotifications([]);setError(null);return}try{setLoading(true);setError(null);const data=await notificationApi.list();setNotifications(Array.isArray(data)?data:[])}catch(cause){setError(getApiErrorMessage(cause,'Không thể tải thông báo'))}finally{setLoading(false)}},[isAuthenticated]);useEffect(()=>{void reload();if(!isAuthenticated)return;const id=setInterval(()=>void reload(),30000);return()=>clearInterval(id)},[reload,isAuthenticated]);const markRead=useCallback(async(id:string)=>{await notificationApi.markRead(id);setNotifications(v=>v.map(n=>n._id===id?{...n,isRead:true}:n))},[]);const markAllRead=useCallback(async()=>{await notificationApi.markAllRead();setNotifications(v=>v.map(n=>({...n,isRead:true})))},[]);const value=useMemo(()=>({notifications,unreadCount:notifications.filter(n=>!n.isRead).length,loading,error,reload,markRead,markAllRead}),[notifications,loading,error,reload,markRead,markAllRead]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useNotifications(){const value=useContext(Context);if(!value)throw new Error('useNotifications must be used inside NotificationProvider');return value}
