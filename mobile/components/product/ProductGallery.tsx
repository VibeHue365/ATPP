import { useState } from 'react';import { Dimensions,NativeScrollEvent,NativeSyntheticEvent,ScrollView,StyleSheet,Text,View } from 'react-native';import { Image } from 'expo-image';import { Colors,FontFamily } from '@/constants/theme';import { getMediaUrl } from '@/utils/media';
const width=Dimensions.get('window').width;
export function ProductGallery({images}:{images:string[]}){const [index,setIndex]=useState(0);const onScroll=(e:NativeSyntheticEvent<NativeScrollEvent>)=>setIndex(Math.round(e.nativeEvent.contentOffset.x/width));return <View style={s.root}>{images.length?<ScrollView horizontal pagingEnabled onMomentumScrollEnd={onScroll} showsHorizontalScrollIndicator={false}>{images.map((uri,i)=><Image key={`${uri}-${i}`} source={getMediaUrl(uri)} style={s.image} contentFit="cover" transition={150}/>)}</ScrollView>:<View style={s.empty}><Text style={s.emptyText}>ẢNH ÁO DÀI</Text></View>}<View style={s.counter}><Text style={s.counterText}>{index+1}/{Math.max(images.length,1)} ảnh</Text></View></View>}
const s=StyleSheet.create({root:{height:380,backgroundColor:'#EEDFE1'},image:{width,height:380},empty:{flex:1,alignItems:'center',justifyContent:'center'},emptyText:{fontFamily:FontFamily.display,fontSize:18,color:Colors.primary},counter:{position:'absolute',right:16,bottom:14,backgroundColor:'#00000088',borderRadius:99,paddingHorizontal:10,paddingVertical:5},counterText:{fontFamily:FontFamily.bodyMedium,fontSize:10,color:Colors.white}});
