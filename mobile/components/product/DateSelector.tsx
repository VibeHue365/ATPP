import { ScrollView,Pressable,StyleSheet,Text,View } from 'react-native';import { Colors,FontFamily,Radius } from '@/constants/theme';
const iso=(d:Date)=>{const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`};
const days=Array.from({length:30},(_,i)=>{const date=new Date();date.setDate(date.getDate()+i+1);return{value:iso(date),day:date.getDate(),month:date.getMonth()+1,week:['CN','T2','T3','T4','T5','T6','T7'][date.getDay()]}});
export function DateSelector({start,end,onChange}:{start:string;end:string;onChange:(start:string,end:string)=>void}){const choose=(value:string)=>{if(!start||end||value<start)onChange(value,'');else onChange(start,value)};return <View><Text style={s.help}>{!start?'Chọn ngày nhận':!end?'Chọn ngày trả':'Chạm ngày khác để chọn lại'}</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.row}>{days.map(item=>{const selected=item.value===start||item.value===end;const between=start&&end&&item.value>start&&item.value<end;return <Pressable key={item.value} onPress={()=>choose(item.value)} style={[s.day,(selected||between)&&s.active]}><Text style={[s.week,selected&&s.activeText]}>{item.week}</Text><Text style={[s.number,selected&&s.activeText]}>{item.day}</Text><Text style={[s.month,selected&&s.activeText]}>Th {item.month}</Text></Pressable>})}</ScrollView></View>}
const s=StyleSheet.create({help:{fontFamily:FontFamily.body,fontSize:11,color:Colors.textMuted,marginHorizontal:16,marginBottom:8},row:{gap:8,paddingHorizontal:16,paddingBottom:4},day:{width:58,paddingVertical:9,alignItems:'center',borderRadius:Radius.md,borderWidth:1,borderColor:Colors.border,backgroundColor:Colors.surface},active:{backgroundColor:Colors.primary,borderColor:Colors.primary},week:{fontFamily:FontFamily.bodyMedium,fontSize:9,color:Colors.textMuted},number:{fontFamily:FontFamily.bodyBold,fontSize:17,color:Colors.text,marginVertical:2},month:{fontFamily:FontFamily.body,fontSize:8,color:Colors.textMuted},activeText:{color:Colors.white}});
