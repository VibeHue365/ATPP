{
  "name": "vibehue-root",
  "version": "1.0.0",
  "description": "VibeHue root project runner",
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
